##ArrayLists

ArrayLists are more dynamic than simple arrays due to their predefined size. In array list it is easy to add or remove data without worrying of the capacity(but initially it's 10)

This program holds a simple application calling employees inside main method and storing them in arrayList.
Also, performing some common actions on the list.
